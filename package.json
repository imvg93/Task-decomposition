{
  "name": "task-decomposition-engine",
  "version": "1.0.0",
  "description": "Rule-based Task Decomposition Engine — structured tasks, dependencies, feasibility",
  "main": "api/index.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "test": "cd backend && npm test"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.1.5"
  },
  "engines": {
    "node": ">=18.x"
  }
}
